// script.js

// Example function to log current date and time
function logCurrentDateTime() {
    const currentDateTime = new Date().toISOString();
    console.log('Current Date and Time (UTC):', currentDateTime);
}

// Call the function
logCurrentDateTime();